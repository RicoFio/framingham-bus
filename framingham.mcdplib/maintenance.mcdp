mcdp {
  implements `maintenance_interface

  required maintenance_cost >= (
    (required economic_parameters).maintenance_cost_per_km * provided distance +
    (required economic_parameters).maintenance_cost_per_round * provided rounds_used
  )
}
