mcdp {
  implements `fuel_interface

  required fuel_cost >= (
    (required economic_parameters).fuel_cost_per_km * provided distance
  )
  required emissions >= (
    (required economic_parameters).emissions_per_km * provided distance
  )
}
